# Documentation only — `wxt build` uses mode=production but must still stay on localhost.
# Ship the live origin with `pnpm zip:store` (VITAL_EXTENSION_TARGET=store).
WXT_API_URL=https://vital.aimo.plus
WXT_WEB_URL=https://vital.aimo.plus
WXT_S3_ENDPOINT=https://s3.aimo.plus
