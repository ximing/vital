{
  "name": "@vital/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Tauri 2 shell that opens the Vital site. Login is the site cookie, same as the browser.",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri",
    "test": "node --test __tests__/tauri.contract.test.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.0"
  }
}
