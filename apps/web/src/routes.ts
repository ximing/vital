/** App path constants. Copy lives in `copy.ts`; routes stay here so screens can import paths without UI strings. */
export const HOME_PATH = '/today';
export const TODOS_HOME_PATH = '/todos/lists/smart:today';
